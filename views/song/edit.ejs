<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= song.title %></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit <%= song.title %></h1>
    <form action="/users/<%= user._id %>/playlists/<%= playlist._id %>/song/<%= song._id %>?_method=PUT" method="POST">
        <label for="title">Title: </label>
        <input type="text" name="title" id="title" value="<%= song.title %>">
        <label for="artist">Artist: </label>
        <input type="text" name="artist" id="artist" value="<%= song.artist %>">
        <label for="genre">Genre: </label>
        <input type="text" name="genre" id="genre" value="<%= song.genre %>">
        <button type="submit">Save Changes</button>
    </form>
</body>
</html>